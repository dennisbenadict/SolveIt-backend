namespace SolveIt.Worker.Execution;

public static class FakeRunner
{
    public static string Execute(string code, string input)
    {
        // Temporary stub
        // Replace with Docker sandbox execution later
        return input;
    }
}
